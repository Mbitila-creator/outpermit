from django.apps import AppConfig


class LearningEventsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "learning_events"
    verbose_name = "Seminars, workshops and training"

